// Svelte's defaults are what this app wants, but vite-plugin-svelte announces the fallback on
// every dev, build and test run when the file is absent, so it stays as the empty default and
// as the place any future compiler or preprocessor option belongs.
export default {};
